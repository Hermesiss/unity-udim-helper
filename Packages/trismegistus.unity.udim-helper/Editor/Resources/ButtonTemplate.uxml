<UXML xmlns="UnityEngine.UIElements">
    <!-- Creates the button. -->
    <Button class="texture-mapper-button">
        <!-- Adds a VisualElement child (corresponding to the button's icon). -->
        <VisualElement class="texture-mapper-button-icon"/>
    </Button>
</UXML>